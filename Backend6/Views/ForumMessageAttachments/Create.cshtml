@model Backend6.Models.ForumViewModels.ForumMessageAttachmentModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>ForumMessageAttachment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-route-messageId="@ViewBag.ForumMessage.Id" asp-route-forumTopicId="@ViewBag.ForumTopic.Id" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="File" class="col-md-2 control-label"></label>
                <div class="col-md-10">
                    <input type="file" asp-for="File" class="form-control" />
                    <span asp-validation-for="File" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Details" asp-controller="ForumTopics" asp-route-id="@ViewBag.ForumTopic.Id">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
